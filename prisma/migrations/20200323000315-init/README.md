# Migration `20200323000315-init`

This migration has been generated by KennHuang at 3/23/2020, 12:03:15 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "quaint"."User" (
    "cardUUID" TEXT NOT NULL DEFAULT '' ,
    "id" INTEGER NOT NULL  ,
    "name" TEXT NOT NULL DEFAULT '' ,
    "userNumber" TEXT NOT NULL DEFAULT '' ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "quaint"."CheckinLog" (
    "checkinAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    "id" INTEGER NOT NULL  ,
    "temperature" REAL NOT NULL DEFAULT 0 ,
    "user" INTEGER NOT NULL  ,
    PRIMARY KEY ("id"),FOREIGN KEY ("user") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
) 

CREATE TABLE "quaint"."AdminAccounts" (
    "id" INTEGER NOT NULL  ,
    "passwordHash" TEXT NOT NULL DEFAULT '' ,
    "salt" TEXT NOT NULL DEFAULT '' ,
    "userName" TEXT NOT NULL DEFAULT '' ,
    PRIMARY KEY ("id")
) 

CREATE UNIQUE INDEX "quaint"."User.userNumber" ON "User"("userNumber")

CREATE UNIQUE INDEX "quaint"."AdminAccounts.userName" ON "AdminAccounts"("userName")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200323000315-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,30 @@
+datasource sqlite {
+  url      = "file: data.db"
+  provider = "sqlite"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id          Int          @id
+  userNumber  String       @unique
+  cardUUID    String
+  name        String
+  checkinLogs CheckinLog[]
+}
+
+model CheckinLog {
+  id          Int      @id
+  user        User     @relation(references: [id])
+  checkinAt   DateTime
+  temperature Float
+}
+
+model AdminAccounts {
+  id           Int    @id
+  userName     String @unique
+  passwordHash String
+  salt         String
+}
```


